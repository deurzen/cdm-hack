# Types of values:
#
# Number     -  <number>
# String     -  "string"
# Boolean    -  <ON|OFF>
# Attribute  -  (foreground,background,highlight?)

# Set aspect-ration.
aspect = 0

# Set separator (for multiple widgets output).
separate_widget = ""

# Set tab-length (for textbox tab-conversion).
tab_len = 0

# Make tab-traversal for checklist, etc., include the list.
visit_items = OFF

# Shadow dialog boxes? This also turns on color.
use_shadow = OFF

# Turn color support ON or OFF
use_colors = ON

# Screen color
screen_color = (BLUE,BLACK,ON)

# Shadow color
shadow_color = (BLACK,BLUE,ON)

# Dialog box color
dialog_color = (BLUE,BLACK,OFF)

# Dialog box title color
title_color = (BLUE,BLUE,ON)

# Dialog box border color
border2_color = (BLUE,BLACK,OFF)
border_color = (BLUE,BLACK,OFF)

# Active button color
button_active_color = (BLUE,BLACK,ON)

# Inactive button color
button_inactive_color = (BLUE,BLACK,OFF)

# Active button key color
button_key_active_color = (BLUE,BLACK,ON)

# Inactive button key color
button_key_inactive_color = (BLUE,BLACK,OFF)

# Active button label color
button_label_active_color = (BLUE,BLACK,ON)

# Inactive button label color
button_label_inactive_color = (BLUE,BLACK,OFF)

# Input box color
inputbox_color = (BLACK,BLUE,OFF)

# Input box border color
inputbox_border_color = (BLACK,BLUE,OFF)

# Search box color
searchbox_color = (BLACK,BLUE,OFF)

# Search box title color
searchbox_title_color = (BLUE,BLUE,ON)

# Search box border color
searchbox_border_color = (BLUE,BLUE,OFF)

# File position indicator color
position_indicator_color = (BLUE,BLUE,ON)

# Menu box color
menubox_color = (BLUE,BLACK,OFF)

# Menu box border color
menubox_border_color = (BLUE,BLACK,OFF)
menubox_border2_color = (BLUE,BLACK,OFF)

# Item color
item_color = (BLUE,BLACK,OFF)

# Selected item color
item_selected_color = (BLUE,BLACK,ON)

# Tag color
tag_color = (BLUE,BLACK,ON)

# Selected tag color
tag_selected_color = (BLUE,BLACK,ON)

# Tag key color
tag_key_color = (BLUE,BLACK,OFF)

# Selected tag key color
tag_key_selected_color = (BLUE,BLACK,ON)

# Check box color
check_color = (BLACK,BLUE,OFF)

# Selected check box color
check_selected_color = (BLUE,BLUE,ON)

# Up arrow color
uarrow_color = (BLUE,BLACK,ON)

# Down arrow color
darrow_color = (BLUE,BLACK,ON)

# Item help-text color
itemhelp_color = (BLUE,BLACK,OFF)

# Active form text color
form_active_text_color = (BLACK,BLUE,ON)

# Form text color
form_text_color = (BLUE,BLUE,ON)

# Readonly form item color
form_item_readonly_color = (BLUE,BLUE,ON)
